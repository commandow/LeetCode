#include <string>
#include <algorithm>
#include <vector>
#include <list>
#include <stack>
#include <iostream>

using namespace std;

class Solution {
public:
    int reverse(int x) {
    }
};

int main(int argc, char **argv)
{
    int x = -11;
    Solution test;
    int ret = test.reverse(x);
    cout << ret << endl;

    int f;
    cin >> f;
}